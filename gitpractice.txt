GIｔの学習

ＧＩｔＢｕｓｈを立ち上げる
cd ~/Desktop
↓
cd [フォルダー名]
↓
ls(なんのファイルが入っているか確認できる)

git init(ローカルにリポジトリ作成)
↓
ls -a(aはallの略)　
↓
ls -a .git .gitというのがフォルダ名でその中にいろいろなファイルがあることがわかる

git statut:今のリポジトリの状態がわかる（変更加えられているかいないか）

git log:履歴を確認する　git log --oneline/git log -n3/git log --oneline -n3 

git diff:変更差分の確認